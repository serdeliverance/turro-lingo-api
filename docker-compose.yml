services:
  tl-postgres:
    image: postgres:14-alpine
    environment:
      - "TZ=Europe/Amsterdam"
      - "POSTGRES_USER=root"
      - "POSTGRES_PASSWORD=root"
      - "POSTGRES_DB=tldb"
    volumes:
      - ./docker/postgres/data:/var/lib/postgresql/data
    ports:
      - 45432:5432
